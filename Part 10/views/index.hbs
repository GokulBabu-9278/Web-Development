{{#with person}}
{{#with comments}}
    <h1>{{comment}}</h1>
    <h3 class="date">{{date}}</h3>

{{/with}}
{{/with}}

{{#each values}}
    <h1>{{@index}}.{{this}}</h1>
{{/each}}

<h1>{{person.comments.comment}}</h1>

{{#if person.admin}}
    <h1>This is admin</h1>
    {{else}}
    <h1>This is user</h1>
{{/if}}